[package]
name = "bptree"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.172"
rand = "0.9.2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bptree_benchmark"
harness = false